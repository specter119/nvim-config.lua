# ReadMe for .dotter usage

for linux:

```toml
#.dotter/local.toml
includes = []
packages = ["default"]

[files]

[variables]
```

for windows:

```toml
#.dotter/local.toml
includes = [".dotter/windows.toml"]
packages = ["default"]

[files]

[variables]
```
